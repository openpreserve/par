/**
 * This package contains the domain classes for all PAR Core Base entities and the sub-entities required to describe them.
 * <p>
 * Each Java class represents a JSON definition, and classes should be annotated using gson annotations for the mapping
 * and swagger API annotations for controlling required and default values.
 */

package org.parcore.domain;
